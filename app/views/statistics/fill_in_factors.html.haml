- @factors.each do |fac|
  = form_for fac do |f|
    %br
    .field
      = f.label :name
      = f.text_field :name
    .field
      = f.label :dependency
      = f.text_field :dependency
    .field  
      = f.label  :amount
      = f.text_field :amount
    .field
      = f.label :unit
      = f.text_field :unit
= link_to "Back", :controller => "statistics", :action => "create_answer"
\|
= link_to "Doesn't Save Yet", :controller => "statistics", :action => "index"
