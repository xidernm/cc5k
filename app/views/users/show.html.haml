%table
 %th
 %th
 %tr
  %td{:width => "300"}= image_tag("badge#{@most_recent_badge.badge_id}.gif", :align => "left")
  %td
   %table
    %th
    %th
    %tr
     %td
      %h3
       Agent:
       = "#{@user.firstName} #{@user.lastName}"
    %tr
     %td Agend ID:
     %td= (@user.email.hash.abs * 5).to_s.bytes.take(5).pack('c*')
    %tr
     %td State:
     %td= @user.state

-# - for badge in @user_badges do
-#  - bid = badge.badge_id
-#  = image_tag("badge#{bid+2}.png")